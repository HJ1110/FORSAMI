# A SPIRAL CIRCLE
import turtle
turtle.pensize(5)
turtle.bgcolor('black')
turtle.speed(0)
for i in range(6):
    for colors in ['red','brown','blue','white','yellow','orange','violet','pink','green','purple']:
        turtle.color(colors)
        turtle.circle(150)
        turtle.left(10)
turtle.hideturtle()
